@model sum.Models.User
@{
    ViewData["Title"] = "Profil";
}

<div class="profile-page">
    <div class="profile-card animate-in">
        <div class="profile-header">
            <div class="profile-avatar">
                <i class="fas fa-user-graduate"></i>
            </div>
        </div>
        <div class="profile-body">
            <div class="profile-name">
                <h1>@(Model.FullName ?? Model.Username)</h1>
                <div class="role-badge">
                    <i class="fas fa-shield-alt"></i>
                    @Model.Role
                </div>
            </div>

            <div class="profile-info-grid">
                <div class="profile-info-item">
                    <div class="profile-info-icon" style="background: rgba(37, 99, 235, 0.15); color: #60a5fa;">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="profile-info-text">
                        <div class="info-label">Uživatelské jméno</div>
                        <div class="info-value">@Model.Username</div>
                    </div>
                </div>

                <div class="profile-info-item">
                    <div class="profile-info-icon" style="background: rgba(16, 185, 129, 0.15); color: #34d399;">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <div class="profile-info-text">
                        <div class="info-label">E-mail</div>
                        <div class="info-value">@Model.Email</div>
                    </div>
                </div>

                @if (!string.IsNullOrEmpty(Model.FullName))
                {
                    <div class="profile-info-item">
                        <div class="profile-info-icon" style="background: rgba(124, 58, 237, 0.15); color: #a78bfa;">
                            <i class="fas fa-id-card"></i>
                        </div>
                        <div class="profile-info-text">
                            <div class="info-label">Celé jméno</div>
                            <div class="info-value">@Model.FullName</div>
                        </div>
                    </div>
                }

                <div class="profile-info-item">
                    <div class="profile-info-icon" style="background: rgba(245, 158, 11, 0.15); color: #fbbf24;">
                        <i class="fas fa-calendar"></i>
                    </div>
                    <div class="profile-info-text">
                        <div class="info-label">Registrace</div>
                        <div class="info-value">@Model.CreatedAt.ToString("d. MMMM yyyy", new System.Globalization.CultureInfo("cs-CZ"))</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
